streamlit>=1.28.0
streamlit-option-menu>=0.3.6
streamlit-lottie>=0.0.5
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0

# Note: smtplib and email.mime are built-in Python modules (no installation needed)
# For email functionality, configure environment variable: EMAIL_PASSWORD